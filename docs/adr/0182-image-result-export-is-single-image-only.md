# 图片结果导出只支持单张粒度

在 0180（一次任务产出多张图片结果）的前提下，首版的图片结果导出（0042：分享、保存到系统相册或导出到文件系统）始终以单张图片结果为粒度。导出入口出现在单张图片结果详情，以及任务历史详情中该次结果分组的每一张上；每次动作只复制一张图片文件本身，沿用 0042 不附带任务快照、完整提示词或模型配置的规则。

首版不提供"一键导出本次任务全部图片"或多选批量导出，以避免引入批量分享、部分失败、批量进度等导出编排复杂度。后续若要支持整组或多选导出，需要另行评估批量交互与失败语义并记录决策。
