# 失败任务只能重新填写当前图鉴条目

失败任务可以提供重新填写入口，但不会基于任务快照或历史完整提示词直接再次调用模型。应用只在当前图鉴条目仍存在，且历史任务快照可被当前版本解析出可匹配输入时，打开新的任务表单，并可将历史输入作为可编辑预填建议；使用者确认后创建全新的任务历史，旧失败记录保持不变。如果当前图鉴条目不存在，或恢复导入的旧任务快照无法被当前版本解析为可匹配输入，则不能从该失败任务重新填写。
