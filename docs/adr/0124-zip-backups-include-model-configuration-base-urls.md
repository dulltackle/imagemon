# ZIP 备份包含模型配置 base URL

ZIP 备份中的非敏感模型配置包含配置名称、模型类型、模型服务、模型名称、调用选项和自定义 base URL 等连接信息。base URL 不作为 API Key 处理，需要进入备份以便恢复配置和解释任务历史；即使 base URL 看起来包含 query token、用户名密码或其他敏感片段，ZIP 导出也不做自动敏感扫描、拦截或额外处理。API Key 仍不进入备份，恢复后的模型配置仍为未就绪状态。
