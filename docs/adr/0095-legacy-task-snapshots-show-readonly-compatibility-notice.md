# 旧任务快照显示只读兼容提示

当恢复导入的任务快照包含当前版本无法解析为可执行图鉴条目的旧图鉴条目内容或旧模板契约时，历史详情页必须显示只读兼容提示，说明该历史快照来自旧版本，仅供查看，不能用于再次执行或编辑。任务历史列表不显示旧快照兼容标记，继续只展示时间、状态、图鉴条目名称和来源类型等基础信息。这不是任务失败或恢复错误，不影响任务历史保留；提示的目的只是避免使用者在详情页误以为当前应用还能理解并复用该快照。
