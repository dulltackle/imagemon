---
status: superseded by ADR-0202
---

# ZIP 恢复自动改名冲突的模型配置

ZIP 恢复导入模型配置时，如果导入配置名称与当前同类型模型配置名称冲突，或同一备份包内多个同类型待导入模型配置互相重名，应用可以自动生成导入副本名称，例如追加导入序号。模型配置名称不是图鉴条目那样的执行语义身份，且导入配置均为未就绪状态、不覆盖默认配置，因此不需要逐个询问。图片模型配置和文本模型配置仍按类型分别判断名称唯一性，二者可以同名。恢复预检必须展示将发生的模型配置改名。
