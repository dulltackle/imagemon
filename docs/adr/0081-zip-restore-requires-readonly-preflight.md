# ZIP 恢复必须先完成只读预检

ZIP 恢复在写入任何应用数据前必须先执行只读恢复预检。恢复预检读取 manifest，校验备份格式版本、资产清单和哈希，统计将导入的资产数量，列出模型配置改名、个人图鉴条目名称冲突、应用默认规格是否采用备份值等需要使用者确认的事项。使用者确认恢复前，应用必须基于当前本地状态重新校验预检结果；如果冲突项、改名结果、可导入判断或需要使用者确认的事项发生变化，应用必须回到更新后的预检结果并要求使用者重新确认。只有使用者在当前有效的预检结果页确认后，应用才进入事务和文件暂存导入流程；取消或预检失败不得写入 SQLite、安全存储或应用文件目录。
