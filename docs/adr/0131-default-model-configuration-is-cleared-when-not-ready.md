# 默认模型配置失去就绪状态后清除默认身份

默认模型配置必须始终指向同类型的就绪模型配置。若默认模型配置因为修改 base URL、模型名、调用选项或 API Key 而重新变为未就绪，或再次测试连接失败、无法确认兼容性而失去就绪状态，应用立即清除该类型默认模型配置，不自动选择替代项；重新测试通过也不自动恢复默认身份，用户需要显式设回默认。这样保持“未就绪不能默认”的不变量，避免后续模型调用悄悄使用未经重新确认的配置。
