# 模型配置最近测试成功时间只作为本地状态保存

模型配置测试连接成功后，详情页保存最近测试成功时间，用于解释配置为什么处于就绪状态，并帮助用户判断这次验证的新旧程度。最近测试成功时间不进入 ZIP 备份；它是当前设备上的测试状态，恢复后的模型配置仍为未就绪，需要在当前设备重新填写 API Key 并显式测试。已就绪配置再次测试失败或无法确认兼容性时，清除最近测试成功时间，因为旧成功时间不再代表当前最近一次显式测试结果。
