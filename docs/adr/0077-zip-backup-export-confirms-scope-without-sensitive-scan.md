# ZIP 备份导出前确认范围但不做敏感扫描

ZIP 备份导出前，应用展示备份包含内容和不包含内容，并由使用者确认后生成明文 ZIP。提示需明确备份包含任务历史、任务快照、完整提示词、图片结果、非敏感模型配置、应用默认规格和内部附件，不包含 API Key 和提炼草稿；应用不对备份内容做自动敏感信息扫描，也不加密备份文件。导出确认页展示的内容摘要只用于帮助使用者确认范围，不作为实际导出的数据依据；使用者确认导出时，应用必须按当前本地数据重新读取并生成备份，不能使用旧摘要或旧查询结果生成 ZIP。
