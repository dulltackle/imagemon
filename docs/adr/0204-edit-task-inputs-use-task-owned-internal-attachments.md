# 编辑任务输入复制为任务专属内部附件

首个编辑任务闭环中，设备使用者通过系统相册或文件选择器提供的 `image` 和 `mask` 输入会在创建编辑任务时复制为该任务历史专属的任务历史内部附件，并由任务快照记录附件路径。应用暂不建立可复用任务素材、附件共享表或引用计数，因为首版只需要历史解释能力；如后续要让多条历史共享附件或从应用内图片结果直接发起编辑，需要重新评估删除、缺失文件、备份和恢复边界。
