# 模型调用页面不自动切换已选模型配置

图片任务或模板提炼页面已选中的模型配置如果随后失去就绪状态，页面保留该选择并显示需要重新测试或不可用的原因，同时禁用执行。应用不自动切换到其他默认模型配置；这样可以避免用户以为仍在使用原配置，实际调用却悄悄换成另一个模型服务或模型名。

如果已选模型配置因测试连接无法确认兼容性而失去就绪状态，页面也按同一规则处理。由于无法确认兼容性不作为最近测试结果或失败摘要持久保存，离开模型配置详情页后，图片任务或模板提炼页面只展示通用“配置未就绪，需要重新测试”原因，不展示专门的无法确认兼容性说明。
