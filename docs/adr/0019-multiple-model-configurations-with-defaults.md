# 支持多个模型配置并设置默认配置

手机应用允许保存多个图片模型配置和多个文本模型配置，并分别设置默认图片模型配置与默认文本模型配置。图片任务默认使用默认图片模型配置，也允许执行前切换；模板提炼默认使用默认文本模型配置。该选择支持不同 OpenAI 兼容平台、模型能力和费用档位，同时通过默认配置保持首版任务执行路径简洁；任务快照仍只保存非敏感的执行时模型配置快照，不保存 API Key 或安全存储引用。
