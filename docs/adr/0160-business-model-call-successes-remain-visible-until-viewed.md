# 业务模型调用成功在相关入口保留待查看状态

图片任务或模板提炼在使用者离开等待页后成功时，首版不发送本地通知，也不自动跳转，但需要在相关入口保留轻量的待查看或待确认状态，直到使用者进入对应结果或处理页面。图片任务成功后，图片和历史相关入口可以显示有新结果状态，查看结果或任务历史后清除；模板提炼成功后，图鉴相关入口显示有提炼方案待确认，进入提炼页后清除。该状态只是应用内可发现性提示，不作为长期通知记录保存，也不进入 ZIP 备份。
