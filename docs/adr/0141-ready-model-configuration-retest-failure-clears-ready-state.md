# 就绪模型配置再次测试未通过后清除就绪状态

已就绪模型配置如果用户再次显式测试连接且测试失败，配置立即变为未就绪，清除最近测试成功时间，并保存模型配置测试失败摘要；HTTP 401、HTTP 403、HTTP 429 与 HTTP 5xx 也按该规则处理。若再次测试得到无法确认兼容性，配置同样立即变为未就绪并清除最近测试成功时间，但不保存新的失败摘要。若该配置是默认模型配置，也按既有规则清除默认身份；最新显式测试未通过或无法确认可用时，不能继续依赖旧的成功测试结果。
