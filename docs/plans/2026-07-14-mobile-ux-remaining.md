# 移动端 UX 盘点剩余任务（2026-07-14）

## 来源与状态

- **来源**：[2026-07-13 移动端 UI/UX 全面盘点](2026-07-13-mobile-ux-audit.md)，条目编号沿用原报告。
- **已完成**（不在本计划中）：
  - 第一批（`improving-ux` 分支，[方案](2026-07-13-mobile-ux-batch-1.md)）：1.1、1.2（部分，见遗留）、3.1、1.4、1.5、1.3，顺带消化 1.6、1.7、1.8；
  - 第二批（同分支，PR #5，[方案](2026-07-13-mobile-ux-batch-2.md)）：2.3、3.2、7.1、6.1、4.1、2.1、2.2、5.1。
- **移交水彩方案**（不在本计划执行）：2.4、2.5、2.7，见[水彩视觉系统方案](2026-07-13-mobile-watercolor-style-implementation.md) 3.5 节。
- **状态核实**：2026-07-14 在 `watercolor-style` 分支逐条 grep 代码确认，行号以当日代码为准。

---

## 一、交互与文案打磨任务（可打包为一个任务执行）

与水彩视觉方案正交，可在水彩任意阶段并行。全部为 P2、成本小。

### 术语与文案

| 编号 | 位置 | 问题 | 建议 |
| --- | --- | --- | --- |
| 3.3 | `app/history/[id].tsx:507` | 区块标题「失败摘要」，canonical 术语是「错误摘要」；`原因 network_error`、`质量 auto` 等原始枚举直接展示 | 术语对齐 + reason/quality 中文标签映射 |
| 4.3 | `app/images/[id].tsx:510` | 「基础规格」非领域术语 | 改「图片规格」 |
| 8.1 | `src/promptdex/TemplateRefinementScreen.tsx:816,827` | 写入信息字段标签为英文小写 `name`、`description` | 改「名称」「说明」，必要时括注契约字段名 |
| 8.3 | `src/promptdex/TemplateRefinementScreen.tsx:938` | 「重新生成」按钮实际只是返回编辑态 | 改文案「返回修改输入」或真的直接重发 |
| 1.9 | `src/promptdex/PromptdexEntryDetailScreen.tsx:1302,1587` | `input.description` 在卡片上方与 placeholder 双写 | 去重，二选一 |

### 行为与交互

| 编号 | 位置 | 问题 | 建议 |
| --- | --- | --- | --- |
| 2.6 | 全局（约 158 处） | 几乎所有 Text 都 `selectable`，含按钮标签、徽章、日期；Android 长按选中与 Pressable 手势冲突 | 仅正文/提示词类文本保留 |
| 3.4 | `app/(tabs)/(history)/history.tsx:233` | 失败/未知任务缩略位用 `photo` 图标占位，视觉上像"有图未加载" | 按状态用差异化占位（如警示图标）；**需落在水彩方案的中性 `MediaFrame` / media matte 契约之内** |
| 7.2 | `src/model-configurations/ModelConfigurationEditor.tsx:424,679` | 新建时也展示「未保存凭据 + 清除凭据」，无凭据可清 | 仅编辑已有配置且 `hasCredential` 时显示 |
| 7.4 | `app/model-configurations/index.tsx:60,70` | 两个「新建」按钮占列表页顶部主视觉，低频操作过重 | 移到列表尾部或导航栏；**按钮样式统一等水彩 `AppButton`，本任务只调位置与层级** |
| 6.2 | `src/first-run/index.tsx:431` | 「使用相同连接信息」只在切换瞬间复制、之后不同步，语义模糊 | 开启时锁定并持续同步，或改一次性「复制图片模型连接信息」按钮 |
| 8.2 | `src/promptdex/TemplateRefinementScreen.tsx:713` | 外部完整提示词无「粘贴」快捷按钮（placeholder 即"粘贴完整提示词"） | 输入框旁加一键粘贴 |

---

## 二、第一批遗留（1.2 应用默认规格的未尽项）

| 事项 | 现状 | 前置条件 |
| --- | --- | --- |
| 质量/格式/数量在任务表单可编辑 | `PromptdexEntryDetailScreen.tsx:1386` 仍是固定文案「质量 自动 · 格式 PNG · 数量 1」 | 无，可直接做（成本中） |
| ADR 0039 规格 × 模型能力校验 | 未实现 | **等 ADR 0221 模型能力档案落地，暂缓** |

---

## 三、Roadmap 备忘（非缺陷，按产品节奏排期）

| 编号 | 事项 | 备注 |
| --- | --- | --- |
| 5.2 | 个人条目编辑展示信息（ADR 0053）、单条导出（ADR 0044）、Markdown 导入（ADR 0005/0061）、搜索/筛选（ADR 0049/0209 已决策延后）、设置页 ZIP 备份/恢复入口（ADR 0008/0061） | 多为独立功能，逐项立项 |
| 4.2 | 图片结果导出补全：分享、导出到文件系统（现仅保存相册） | CONTEXT.md 定义三种方式 |
| 7.3 | 模型配置测试连接可取消（ADR 0146/0175） | 成本中 |
| ~~2.7~~ | ~~大屏 max-width~~ | 已由水彩方案吸收（4.6 节容器规则 + `ScreenCanvas` 最大宽度） |

---

## 四、勘误记录

- **1.6（`bg-blue-50` 逃逸点）经核实已随第一批顺带修复**，当前代码中已无 `bg-blue-50`。此前审计文档与水彩方案 3.5 节将其列为"由水彩迁移第 9 步清理"，已过时，两处已于 2026-07-14 同步修正。
