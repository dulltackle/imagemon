创建一张 390×844 比例、纵向、完整未裁切的移动应用高保真 UI 概念稿。它是 Expo React Native 应用 Imagemon 的“模板提炼”待审阅工具页，不是网页、海报、营销页或手机外壳。所有中文和英文必须清晰准确，画面必须像原生代码可实现的真实应用截图。

顶部保留真实状态栏和原生 Stack 导航栏：左侧返回箭头，居中标题“模板提炼”。页面固定浅色，背景 #faf8f5，工具表面和字段 #fffdfb / #ffffff，正文 #3a3430，次文字 #6f6964，主操作 #4a6fa5，细边框 rgba(74,111,165,0.18)。系统 sans 字体，20px 横向 gutter，工具卡圆角 16px，输入框圆角 14px，几乎无阴影。整个页面是中性工具层，不使用水彩 wash、插画、纸纹、彩色 overlay、渐变或玻璃效果。

保持现有审阅顺序，页面可滚动。首屏先显示工具卡标题“写入信息”。卡内依次显示标签“name”和单行字段，值为“refined-light-poster”；标签“description”和多行字段，值为“将完整提示词提炼为可复用的浅色海报图鉴条目”。字段边界清楚、纯白、无装饰。

下一张工具卡标题“方案摘要”，显示简短正文“识别为生成任务，并保留主体、构图与文字约束。”，子标题“保留规则”，下面两行摘要“保留明确的视觉风格与构图要求”“保留输出文字与尺寸约束”；子标题“删除规则”，下面显示“移除一次性示例与无关背景叙述”。

再下方露出工具卡标题“提炼补充”和第一条补充摘要，表明页面仍可继续向下滚动；不要把完整长正文硬塞进首屏。画面底部可以看到两个原生按钮：次按钮“重新生成”和主按钮“写入个人图鉴”，主按钮因尚未勾选批准而使用清楚可读的 disabled 状态。可以在按钮上方露出未勾选复选框文字“我已检查将写入的完整正文和输入声明。”的顶部，但不要发明新的确认项。

不要添加“AI 建议”、评分、置信度、聊天气泡、步骤导航、进度条、Tab bar 或新业务文案。审阅内容密度高但结构稳定，强调可读、可验证和中性工具边界。
